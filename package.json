{
	"name": "lampas",
	"private": true,
	"workspaces": ["packages/*"],
	"scripts": {
		"build": "npm run build --workspaces",
		"check": "biome check .",
		"check:fix": "biome check --write .",
		"test": "vitest run"
	},
	"devDependencies": {
		"@biomejs/biome": "^1.9.0",
		"typescript": "^5.7.0",
		"vitest": "^3.0.0"
	},
	"engines": {
		"node": ">=20"
	}
}
